/**
 * Legacy export — ticket detail now uses TicketDetailTimeline.
 * Kept so older imports keep working with the human-friendly formatter.
 */
export { TicketDetailTimeline as TicketAuditHistory } from '@/components/tickets/detail/ticket-detail-timeline';
