## Parent

#82

## What to build

**Recordatorios de servicio** management page at `/dashboard/service-schedules`: sidebar nav entry (gated on `tickets.read`), list with filters Próximos | Atrasados | Programados | Pausados | Todos, TanStack table on desktop and cards below `md`, columns for Client, Service, interval, last service, next due. Add/edit dialog with interval presets (1, 2, 3, 6, 12 meses, 1 año) and Personalizado (months/days). Upsert on save (no duplicate Client+Service). Pause/resume with optional reason; soft-delete with confirm. Mutations require `tickets.write` or `clients.write`; read-only otherwise. Spanish copy. Company context for system Company users.

## Acceptance criteria

- [ ] Sidebar item “Recordatorios de servicio” visible with `tickets.read`; page forbidden without it
- [ ] List loads schedules for effective Company; filters match PRD bucket rules
- [ ] Add/edit upserts single row per Client+Service; presets and custom interval work
- [ ] Pause, resume, delete (soft) work and update list without stale paused items in Próximos/Atrasados
- [ ] Mobile card layout and desktop table per list responsive rules
- [ ] Browser: open page, filter Próximos, add schedule, edit, pause—on preview or local dev

## Blocked by

- #83
